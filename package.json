{
  "name": "promptforge-governance",
  "private": true,
  "description": "PromptForge governance SDKs and platform PEP plugins (Hermes, OpenClaw, …)",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "test": "pnpm -r test",
    "test:conformance": "pnpm --filter @promptforge/governance-pdp test -- conformance && python3 -m unittest discover -s hermes-plugin -p 'test_*.py'",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "build:record-pep-build": "python3 scripts/record_build.py"
  },
  "engines": {
    "node": ">=18.18"
  }
}
